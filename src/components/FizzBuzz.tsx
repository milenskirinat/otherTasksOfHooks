import { useState, useEffect } from 'react';

export default function FizzBuzz() {
  const [num, setNum] = useState(0);
  const [] = useState([])

  useEffect (() => {
	if number === 5 
		fskldlfsldfl
	if else number === 3
		fkdskfsdkfk
	if numebr 3 || 5 
		fksdlfls
	else 

  }, [])
const ar = []
  for (let i = 0; i <= 5; i++) {
	if 1231 == 3
		ar.push('Fizz')
	ar.push(i);
  }
  return (
    <div>
      <input type="number" onChange={} />
	    <p >Entered number: {5}</p>
	    <div>{здесь выводим список в любом удобном формате}</div>
    </div>
  );
}